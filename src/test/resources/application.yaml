#List of application properties:
spring:
  profiles:
    active: local
  application:
    name: izicap-task

# Documentation for Sirene API integration: https://entreprise.data.gouv.fr/api_doc/sirene
sirene:
  url: https://entreprise.data.gouv.fr/api/sirene/
  # timeout in milliseconds
  timeout: 10000